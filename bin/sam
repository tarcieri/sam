#!/usr/bin/env ruby

# Hack to allow the 'sam' script to work from a source code checkout
devel_path = File.expand_path('../../lib', __FILE__)
$LOAD_PATH << devel_path if File.exists? File.expand_path('sam.rb', devel_path)

require 'sam'
require 'sam/manager'

Sam::Tty.ohai "Sam-I-Am!"